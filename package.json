{
  "name": "vscode-image-copy-to-clipboard-webview",
  "displayName": "vscode-image-copy-to-clipboard-webview",
  "description": "repo of the issue",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.108.1"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "vscode-image-copy-to-clipboard-webview.showImage",
        "title": "Show Image in Webview"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.108.1",
    "@types/node": "22.x",
    "typescript": "^5.9.3"
  }
}
